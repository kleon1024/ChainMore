version: '2'
services:
  chainmore:
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
    environment:
      - VIRTUAL_HOST=api.kleon.space
      - LETSENCRYPT_HOST=api.kleon.space
